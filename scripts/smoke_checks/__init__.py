"""Scenario modules for scripts/smoke.py.

Each module exposes build_checks(tmp_dir, python) which writes the synthetic
fixtures it needs under tmp_dir and returns its ordered list of checks.
All fixture data in this package is synthetic.
"""
